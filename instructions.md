Remember, a boolean expression is an expression which results in either true or false (a boolean value) when it is evaluated.

Take a look at the following boolean expressions provided and rewrite each to make it evaluate to the opposite boolean value. In other words, if it evaluates to true, change the expression so that it evaluates false.

Before starting this exercise, click Run, and notice that your output will be the following...

`true`

`true`

`true`

`false`

`false`

`false`

After completing the exercise, your code should run with output that looks something like the following...

`false`

`false`

`false`

`true`

`true`

`true`

`true`

`true`

`true`

**Hint:** Don't just rush through this exercise as fast as you can. Experiment a little bit. See what different things you can try and observe how they affect your boolean expressions.